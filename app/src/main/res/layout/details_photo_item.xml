<?xml version="1.0" encoding="utf-8"?>

<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:elevation="@dimen/spacing_extra_small"
    android:src="@drawable/estate"
    app:cardCornerRadius="0dp">

    <ImageView
        android:id="@+id/detailsPhotoItemImageView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:contentDescription="@string/estate_photo"
        android:scaleType="centerCrop"
        android:src="@drawable/estate" />

    <TextView
        android:id="@+id/detailsDescriptionTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:background="@color/semi_transparent_dark_shade"
        android:gravity="center"
        android:padding="@dimen/spacing_extra_small"
        android:textColor="@color/white"
        tools:text="Kitchen" />
</com.google.android.material.card.MaterialCardView>


