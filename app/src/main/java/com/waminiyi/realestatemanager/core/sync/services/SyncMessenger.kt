package com.waminiyi.realestatemanager.core.sync.services

import com.waminiyi.realestatemanager.core.messenger.RemMessenger
import javax.inject.Inject

class SyncMessenger @Inject constructor(private val remMessenger: RemMessenger) {

}